rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Images folder rules
    match /images/{imageId} {
      // Allow public read access for images (needed for browser img tags)
      allow read: if true;

      // Allow write if user is authenticated and file meets requirements
      allow write: if request.auth != null
        && request.resource.size < 5 * 1024 * 1024  // 5MB max file size
        && request.resource.contentType.matches('image/.*')  // Only image files
        && request.resource.contentType in ['image/jpeg', 'image/png', 'image/gif', 'image/webp', 'image/jpg'];
    }

    // Default: deny all other access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
